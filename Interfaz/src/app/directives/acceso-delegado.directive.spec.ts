import { AccesoDelegadoDirective } from './acceso-delegado.directive';

describe('AccesoDelegadoDirective', () => {
  it('should create an instance', () => {
    const directive = new AccesoDelegadoDirective();
    expect(directive).toBeTruthy();
  });
});
